{/*import { useState } from "react";
import { useParams } from "react-router";

function Defconfive({ a }) {
    const { c, d } = useParams();
    return (
      <pre>
        {c} <br /> {d}
      </pre>
    );
  }
  
  const b = {
    c: "e",
    d: "f",
  };
  
  export default function Abc() {
    return <Defconfive a={b} />;
  }
*/}

export{}