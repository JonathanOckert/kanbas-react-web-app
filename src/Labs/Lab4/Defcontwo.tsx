{/*function Defcontwo({ a, b }) {
    return (
      <div>
        {a} times {b} = {a * b}
      </div>
    );
  }
  
  export default function Abc() {
    const q = [2, 3];
    const [x, y] = q;
    return <Defcontwo a={x} b={y} />;
  } */}

  export{}