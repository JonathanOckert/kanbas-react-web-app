{/*import { Routes, Route, useParams } from "react-router";
import { Link } from "react-router-dom";

function Def() {
    const { pathname } = useParams();
    return (
      <div>
        output =
        {pathname.__5__("__6__") && <span>p</span>}
        {pathname.__7__("w") && <span>y</span>}
      </div>
    );
  }
  
  export default function Abc() {
    return (
      <div>
        <Link to="q/__1__">x</Link>
        <Link to="q/s">r</Link>
        <Routes>
          <Route path="q/:a" element={<Def />} />
        </Routes>
      </div>
    );
  }
*/}

export{}